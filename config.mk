# Sample configuration file for Makefile
# Copy this file to config.mk and customize the settings below

# Maximum number of repositories to fetch (default: 100)
# REPO_LIMIT = 500

# Number of top topics to extract (default: 20)
# TOPICS_LIMIT = 30

# Directory to store data files (default: data)
# DATA_DIR = custom-data

# Override external commands if needed
# GH = /usr/local/bin/gh
# JQ = /usr/local/bin/jq
# EMACS = /usr/local/bin/emacs

# Add any other custom settings below